<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"><img src="/img/logo-DH.png" alt="Logo Digital House"></a>
    </figure>
    <%- include('./partials/header')  %> 
    <h1><%= movie.title %></h1>    
    <br>
    <p>RATING: <%= movie.rating %></p>
    <p>AWARDS: <%= movie.awards %></p>
    <p>LENGTH: <%= movie.length + ' min'%></p>
    <p>RELEASE DATE: <%= movie.release_date %></p>
    <br>
    <br>
    <% if (movie.genre) { %>
    <h4>Género: <a href="/genres/detail/<%= movie.genre.id %>"><%= movie.genre.name %></a></h4>
    <% }else{ %>
        <h5 style="color: red;">¡La película no tiene género!</h5>
    <% } %>
    <br>
    <h4>Actores</h4>
    <br>
    <% if (movie.actors) { %>
    <ul>
        <% movie.actors.forEach(actor => { %>
        <li><a href="/actors/detail/<%= actor.id %>"><%= actor.first_name %> <%= actor.last_name %></a></li>
        <% }) %>
    </ul>
    <% }else{ %>
        <h5 style="color: red;">¡La película no tiene actores!</h5>
    <% } %>
    <br>
    <section>
        <a href="/movies/edit/<%= movie.id %>">Editar</a>
        <a href="/movies/delete/<%= movie.id %>">Eliminar</a>
    </section>
</body>
</html>